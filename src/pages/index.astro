---
// Redirect to appropriate language based on browser preference or stored selection
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Ryad</title>
    <style>
      body {
        margin: 0;
        display: flex;
        align-items: center;
        justify-content: center;
        min-height: 100vh;
        background: #09090b;
        color: #a1a1aa;
        font-family: system-ui, -apple-system, sans-serif;
      }
    </style>
    <script>
      // Check localStorage for saved preference
      const stored = localStorage.getItem('locale')
      const lang = (stored === 'en' || stored === 'fr') ? stored : navigator.language.startsWith('fr') ? 'fr' : 'en'
      window.location.href = '/' + lang + '/'
    </script>
    <noscript>
      <meta http-equiv="refresh" content="0;url=/en/" />
    </noscript>
  </head>
  <body>
    <p>Redirecting...</p>
  </body>
</html>
